<h1>Demande de devis</h1>

<p>
  <%= @contact.first_name %> <%= @contact.last_name %> demande un devis sur <%= link_to "alicia-bussereau.com", "https://alicia-bussereau.com"  %> 
</p>

<div>
  <div>
    Les infos sur le client : <br>
    Nom: <%= @contact.first_name %> <%= @contact.last_name %>
    Email: <%= @contact.email %>
    Tel: <%= @contact.phone_number %>
    <% if @contact.city %>
      Ville : <%= @contact.city %>
    <% end %>
  </div>
  <div>
    <p>
      Les informations données pour le devis : <br>
      Lieux de l'évènement : <%= @quoting.address %> <%= @quoting.zipcode %> <%= @quoting.city %>
      Date : <%= @quoting.event_date %>
      Type(s) d'évènements demandé(s) : <%= @quoting.categories.join(", ") %>
    </p>
  </div>
</div>


<p>
  Répondre au message : <%= link_to @contact.email, "#" %><br>
  <% if @contact.phone_number %>
    Appeler <%= @contact.first_name %> : <%= @contact.phone_number %>
  <% end %>
</p>
